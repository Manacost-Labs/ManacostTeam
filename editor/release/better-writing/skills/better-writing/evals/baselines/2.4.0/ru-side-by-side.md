## Исходник

Проверка заняла 42 минуты. Она могла завершиться раньше, однако очередь `billing_sync` дважды ожидала ответа от `/v2/status`. Мы не знаем, почему второй ответ пришёл с задержкой.

## Редакция

Проверка заняла 42 минуты, хотя могла завершиться раньше. Очередь `billing_sync` дважды ожидала ответа от `/v2/status`; причина задержки второго ответа неизвестна.
